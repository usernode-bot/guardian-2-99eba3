{
  "name": "Guardian",
  "secrets": [
    {
      "key": "DATABASE_URL",
      "description": "PostgreSQL connection string for Guardian's database. Format: postgresql://user:password@host:port/database. Required for persistent storage of users, messages, and channels.",
      "required": false,
      "private": true,
      "staging_default": "postgresql://localhost/guardian_staging"
    },
    {
      "key": "APP_PUBKEY",
      "description": "Guardian app's ED25519 public key address (ut1...) for blockchain transactions. Used for transaction signing and app identification on Usernode testnet.",
      "required": true,
      "private": false,
      "default": "ut1Fww7onqF9LsRSb6d6BozgQWtjNYqQJghYxXmBc8foncb"
    },
    {
      "key": "APP_SECRET_KEY",
      "description": "Guardian app's ED25519 private key seed for cryptographic signing of transaction memos on Usernode blockchain. 64 hex characters. Encrypted at rest.",
      "required": true,
      "private": true,
      "staging_default": "guardian_sk_mqudwes5_ae613cf56214808e"
    },
    {
      "key": "NODE_RPC_URL",
      "description": "Usernode blockchain RPC endpoint for transaction submission. Platform provides in-network default; override for custom endpoints.",
      "required": false,
      "private": false,
      "default": "http://usernode-node:3000"
    },
    {
      "key": "NETWORK_MODE",
      "description": "Operating mode for blockchain transactions: testnet (true blockchain with RPC/explorer polling), devnet (database-only, no blockchain), or mainnet (future support). Defaults to devnet. Can be set via Secrets or toggled at runtime in Settings → System → Network Mode (changes persist across restarts).",
      "required": false,
      "private": false,
      "default": "devnet"
    },
    {
      "key": "EXPLORER_URL",
      "description": "Block explorer base URL for polling transaction confirmation status. Defaults to Usernode's testnet explorer. Override for custom testnet deployments or alternate explorer endpoints.",
      "required": false,
      "private": false,
      "default": "https://explorer.testnet.usernodelabs.org"
    },
    {
      "key": "TRANSACTIONS_BASE_URL",
      "description": "Optional: Base URL for transaction API integration with wallet bridge. If not set, dynamically derives from request origin (e.g., https://staging.example.com). If set, use the full transaction API URL including the chain suffix, e.g., https://testnet-explorer.usernodelabs.org/testnet or http://localhost:3000/explorer-api/testnet — the Usernode bridge appends '/transactions' to this URL to poll for on-chain inclusion.",
      "required": false,
      "private": false,
      "staging_default": "http://localhost:3000/explorer-api/testnet"
    }
  ],
  "tests": [
    {
      "name": "Home loads",
      "path": "/",
      "expectSelector": "#conversationsView"
    },
    {
      "name": "Chat thread loads",
      "path": "/",
      "expectSelector": "#messageList"
    },
    {
      "name": "Discover contacts via sidebar",
      "path": "/",
      "expectSelector": "#contactsView"
    },
    {
      "name": "Sidebar toggles",
      "path": "/",
      "expectSelector": "#sidebar"
    },
    {
      "name": "Share contact modal works",
      "path": "/",
      "expectSelector": "#shareContactModal"
    },
    {
      "name": "Settings panel opens",
      "path": "/",
      "expectSelector": "#settingsPanel"
    },
    {
      "name": "Network Mode selector in settings",
      "path": "/",
      "expectSelector": "#networkModeSelect"
    },
    {
      "name": "Guardian Posts view opens",
      "path": "/",
      "expectSelector": "#guardianPostsView"
    },
    {
      "name": "Guardian Posts My Channels tab loads",
      "path": "/",
      "expectSelector": "#guardianMyChannelsList"
    },
    {
      "name": "Guardian Posts Discover tab loads",
      "path": "/",
      "expectSelector": "#guardianDiscoverChannelsList"
    }
  ],
  "visibility": {
    "build": "private",
    "view": "public"
  }
}
